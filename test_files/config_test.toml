# Configuration file with version dependencies
[project]
name = "test-project"
version = "1.0.0"

[dependencies]
# Explicit GitHub type
rust_version = "0.15.0" # [cup] GitHub rezi-labs/rezi-web

# Fallback to remote_default (GitHub)
vscode_version = "0.15.0" # [cup] rezi-labs/rezi-web

# JSON-like TOML syntax with fallback
"node-version" = "0.15.0" # [cup] rezi-labs/rezi-web

[build]
# Assignment with := syntax
docker := "0.15.0" # [cup] rezi-labs/rezi-web

# Colon syntax
typescript: "0.15.0" # [cup] rezi-labs/rezi-web